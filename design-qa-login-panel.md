# 登录页右侧面板 Design QA

## 对照范围

- 参考图（手机登录）：`C:\Users\doraemon\AppData\Local\Temp\codex-clipboard-5879948c-9cbd-453e-9fb8-3029bd86d58a.png`
- 参考图（密码登录）：`C:\Users\doraemon\AppData\Local\Temp\codex-clipboard-aa20acaf-92f4-42e6-88c9-d9a487346b7f.png`
- 现有整页参考：`C:\Users\doraemon\AppData\Local\Temp\codex-clipboard-49487989-41cd-45be-88d0-c270217cb480.png`
- 实现入口：`http://localhost:4173/sign-in`
- 首轮浏览器检查：1280 × 800，简体中文，亮色模式，手机登录默认态。

仓库根目录已有一个未跟踪的 `design-qa.md`，为避免覆盖用户文件，本次报告使用独立文件名。

## 首轮对照结果

### 已符合

- 布局与顺序：右侧顶部为“手机登录 / 密码登录”，手机登录默认选中；输入区、协议区、主题色主按钮和底部辅助入口的顺序与参考方向一致。
- 字体与文案：沿用项目 Public Sans/系统中文字体链和现有字号层级；新增文案已进入六种语言资源。密码账号提示保留“用户名、邮箱或手机号”的既有能力。
- 间距与表面：表单采用 48px 输入框和主按钮、12px 圆角、紧凑的 16px 纵向节奏；登录页专用输入样式不影响注册页。
- 颜色与主题：主按钮、验证码、忘记密码、注册入口均使用项目主题 token，没有写死为黑色。
- 图标与素材：登录输入框按参考移除左侧装饰图标；密码显隐继续使用项目既有 Lucide 图标。没有新增或伪造图片、SVG 或 CSS 插画。
- 动态内容：注册入口、登录总开关、协议、人机验证、Passkey、微信和 OAuth 继续读取现有后台状态。
- 可访问性：模式切换使用 `role="tablist"` / `role="tab"`、`aria-selected` 和原生按钮；输入框保留 label、autocomplete、inputmode 和最大长度语义。

### 已发现并修复

1. **P1 · 颜色/选中态**：主题预设中的 `button[class*="bg-primary"]` 误将 `after:bg-primary` 下划线类识别为主按钮，导致选中的“手机登录”显示为整块橙色背景。修复为透明底边框加 `border-primary`，并增加回归测试，防止标签类名再次包含 `bg-primary`。
2. **P2 · 输入框表面**：旧的 `sf-auth-input` 主题规则会用 `!important` 把登录页新输入框改回灰底、透明边框。修复为登录页独立的 `sf-sign-in-input` 标记，保留白底描边，同时不改变注册页旧样式；已增加隔离测试。

## 自动化验证

- 全量前端测试：100 passed，0 failed。
- 本次改动定向 lint：通过。
- 本次改动定向格式检查：通过。
- Rsbuild 生产构建：通过。
- 全仓库 typecheck、lint 和 format check 仍被本次任务外的既有问题阻断；本次认证文件未出现在错误列表中。

## 此前尚未完成的浏览器检查

修复后再次访问本地页面时，应用内浏览器的 URL 安全策略拒绝访问该本地地址。依照工具安全规则，没有改用其他浏览器或命令行浏览器绕过，因此以下项目未能完成最终截图对照：

- 修复后的 1280px 手机登录与密码登录截图。
- 390px 手机端的两种登录模式。
- 深色模式的两种登录模式。
- 修复后模式切换、字段清空、密码显隐和控制台状态复测。

代码、测试和生产构建均已完成，但最终视觉与交互浏览器验收需要人工打开本地预览补查。

## 2026-07-27 深色验证码按钮回归

- 问题：橙绿主题使用 `button[class*="bg-primary"]` 识别实心主按钮，误命中验证码按钮的 `hover:bg-primary/10`，导致深色模式下验证码按钮被强制填充为橙色。
- 修复：亮色和深色主题均改为精确匹配 `[class~="bg-primary"]`，只让真正包含 `bg-primary` 类的主按钮使用实心橙色；半透明悬停工具类不再被误判。
- 回归保护：新增主题选择器测试，禁止按钮主题规则重新使用 `class*="bg-primary"`。
- 浏览器复查：在隔离的未登录会话中启用深色主题，输入有效测试手机号后确认验证码按钮背景为透明、文字为主题橙色，主登录按钮仍为实心橙色。
- 工程验证：前端全量测试 106 项通过，定向 lint 和格式检查通过，生产构建通过。全仓库 typecheck、lint 和 format check 仍受本次任务外的既有问题影响。

### 密码登录输入框颜色一致性

- 问题：密码组件固定携带注册页使用的 `sf-auth-input`，即使登录页传入 `sf-sign-in-input` 仍会被旧的 `!important` 规则覆盖，导致账号框和密码框使用不同的深色表面。
- 修复：密码组件增加显式 `sign-in` 外观，登录页不再混入旧样式标记；橙绿主题补齐登录页专用的默认、悬停和聚焦规则。
- 回归保护：组件测试确认登录外观只输出 `sf-sign-in-input`，不再输出 `sf-auth-input`。
- 浏览器复查：桌面尺寸和用户截图对应的 554 × 673 尺寸均通过。深色默认态的两个输入框统一为 4% 白色表面和 14% 白色描边；聚焦框使用 8% 白色表面、主题橙色描边和 15% 橙色焦点环。

final result: 深色验证码按钮与密码登录输入框回归通过；此前记录的完整桌面、移动端双模式矩阵仍需另行补查
