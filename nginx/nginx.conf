# Nginx 配置 for New-API
# 此文件应放置于 ./nginx/nginx.conf
# docker-compose 会将其挂载到 nginx 容器中

# HTTP → HTTPS 重定向（如果启用了 SSL）
# 暂时注释掉，因为没有 SSL 证书
# 申请到证书后，取消注释并启用下面的 HTTPS server 块
# server {
#     listen 80;
#     server_name modelrec.net;
#
#     # Let's Encrypt 验证（申请 SSL 证书时需要）
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     # 所有 HTTP 流量重定向到 HTTPS
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# HTTPS 配置（需要 SSL 证书）
# 首次部署时可以先注释掉这个 server 块，等申请到证书后再启用
    # server {
    #     listen 443 ssl http2;
    #     server_name modelrec.net;  # 改为你的域名
#
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers        HIGH:!aNULL:!MD5;
#
#     # 静态文件由 Nginx 直接提供（如果前端文件已挂载）
#     location / {
#         root /usr/share/nginx/html/default;
#         try_files $uri $uri/ /index.html;
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     # API 请求转发到 modelrec.net 容器
#     location /api/ {
#         proxy_pass http://modelrec.net:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # 健康检查（不需要转发）
#     location /health {
#         access_log off;
#         return 200 "ok\n";
#         add_header Content-Type text/plain;
#     }
#
#     # SSE / 流式 API（必须关闭缓冲）
#     location /v1/chat/completions {
#         proxy_pass http://modelrec.net:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Connection "";
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_read_timeout 300s;
#     }
# }

# HTTP 配置（开发/测试用，或没有 SSL 证书时）
server {
    listen 80;
    server_name modelrec.net;

    # 健康检查
    location /health {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    # API 请求转发到 new-api 容器
    location /api/ {
        proxy_pass http://new-api:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态文件（如果前端已构建并挂载）
    location / {
        root /usr/share/nginx/html/default;
        try_files $uri $uri/ /index.html;
        
        # 缓存策略
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 静态资源缓存优化
    location ~* \.(js|css)$ {
        root /usr/share/nginx/html/default;
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location ~* \.(png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /usr/share/nginx/html/default;
        expires 365d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # SSE / 流式 API
    location /v1/chat/completions {
        proxy_pass http://new-api:3000;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }

    # WebSocket 支持
    location /ws/ {
        proxy_pass http://new-api:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;
}
